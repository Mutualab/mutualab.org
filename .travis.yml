language: node_js     
node_js:
- stable
branches:
  only:
  - master
before_script:
- npm install -g bower
- npm install gulp
- bower install
script: git fetch origin gh-pages; git checkout -b gh-pages; git checkout master;
cache:
- node_modules
- bower_components
  
