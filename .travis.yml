language: node_js     
node_js:
- stable
branches:
  only:
  - master
before_script:
- npm install -g bower
- npm install gulp
- npm install
- bower install
script: git checkout -b gh-pages origin/gh-pages; git checkout master; gulp build:gh-pages
cache:
- node_modules
- bower_components
  
